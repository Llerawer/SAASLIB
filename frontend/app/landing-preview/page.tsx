import { HeroStage } from "@/components/landing/hero-stage";

export default function LandingPreviewPage() {
  return (
    <main className="min-h-screen flex items-center py-16 md:py-24">
      <HeroStage />
    </main>
  );
}
